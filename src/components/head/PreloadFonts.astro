---
const { fonts } = Astro.props;
---

<!-- Preloads -->{
  fonts.map((font: string) => (
    <link
      rel="preload"
      href={`/fonts/${font}.woff2`}
      as="font"
      type="font/woff2"
      crossorigin="anonymous"
    />
  ))
}

---
interface Props {
  time: string; // Expects "HH:MM"
}

const { time } = Astro.props;

// Just split the strings; no need to parse them as numbers
const [hour, minutes] = time.split(":");
---

<time datetime={time}>{hour}:{minutes}</time>
